#include <stdio.h>

int main() {

    // Constant = valor fixo que não pode ser alterado pelo programa durante sua execução.

    const float PI = 3.14159;

    //PI = 420.69;  // Resultará em erro caso PI seja alterado.  
    
    printf("%f\n", PI);

    return 0;
}
